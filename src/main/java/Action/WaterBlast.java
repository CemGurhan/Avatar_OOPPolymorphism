package Action;

import Element.Avatar;

public class WaterBlast extends Blast implements IBlast {

    public WaterBlast(int distance, int amount, Avatar avatar){
        super(distance,amount,avatar);
    }
}
